# 구현 및 실행 가이드

## 1. 현재 완성 상태

| 항목 | 상태 |
|---|---|
| C11 기반 멀티 ECU 참조 모델 | ✅ 구현·빌드 완료 |
| COM → PduR → CanIf → CAN 계층 로그 | ✅ 구현 완료 |
| ECU1 100ms 주기 속도 송신 | ✅ 구현 완료 |
| ECU2 수신·복원 | ✅ 구현 완료 |
| 500ms timeout 감시 | ✅ 자동 테스트 통과 |
| 잘못된 CAN ID 거부 | ✅ 자동 테스트 통과 |
| 잘못된 DLC 거부 | ✅ 자동 테스트 통과 |
| 250 km/h 경계값 검증 (251·65535 거부, 250 수락) | ✅ 자동 테스트 통과 |
| 시퀀스 연속성 검증 (중복/역전 카운터 거부) | ✅ 자동 테스트 통과 |
| GitHub Actions CI | ✅ 구성 완료 |
| A-SPICE 스타일 SWE.1~SWE.5 문서 | ✅ 작성 완료 |
| 요구사항↔소스↔테스트 추적성 | ✅ CSV 작성 완료 |
| TOPPERS upstream 커밋 고정 | ✅ 스크립트 구현 완료 |
| 실제 RH850/Athrill 실행 증거 | ✅ 검증 완료 — `evidence/atk2-athrill.log` (양쪽 ECU 배너 + 각 150 COM 사이클) |

## 2. 실행

Linux, macOS 또는 WSL에서 다음을 실행합니다.

```bash
cd autosar-multiecu-portfolio
make
make test
make evidence
```

개별 고장 시나리오:

```bash
./build/multiecu_demo normal
./build/multiecu_demo timeout
./build/multiecu_demo invalid-id
./build/multiecu_demo invalid-dlc
./build/multiecu_demo invalid-range
./build/multiecu_demo invalid-seq
./build/multiecu_demo wrap
```

## 3. 실제 ATK2/A-COMSTACK 통합 준비

Docker가 설치된 컴퓨터에서:

```bash
make upstream-setup
make upstream-run
```

그다음 `docs/UPSTREAM_INTEGRATION.md`의 네 터미널 실행 절차를 따릅니다. 실제 실행 로그에 다음 배너가 나타나야 합니다 (2026-09-20 실행에서 양쪽 ECU 모두 확인됨).

```text
TOPPERS/ATK2-SC1 Release 1.4.2 for HSBRH850F1K
```

로그를 `evidence/atk2-athrill.log`로 보존했으므로 문서의 `TC-UP-001`은 PASS입니다 (재실행 시에도 배너 확인 전까지 PASS로 변경하면 안 됩니다).

## 4. GitHub 게시 순서

```bash
git init
git add .
git commit -m "feat: add traceable multi-ECU AUTOSAR communication demo"
git branch -M main
git remote add origin <본인의 GitHub 저장소 URL>
git push -u origin main
```

저장소 소개 문구 권장안:

> Two-ECU vehicle-speed communication demo with an AUTOSAR Classic-style COM/PduR/CanIf/CAN flow, deterministic fault injection, CI, and ASPICE-inspired traceability. Includes a pinned path to TOPPERS ATK2/A-COMSTACK on Athrill.

## 5. 지원서에 넣을 영어 설명

### CV 한 줄

> Developed and verified a two-ECU vehicle-speed communication demo using an AUTOSAR Classic-style COM–PduR–CanIf–CAN architecture, with deterministic fault injection and ASPICE-inspired bidirectional traceability.

### 면접 30초 답변

> I built a two-ECU communication project to demonstrate more than coding. ECU1 transmits vehicle speed every 100 milliseconds, and ECU2 validates the CAN identifier and payload length, decodes the signal, and detects a 500-millisecond communication timeout. I documented the project from software requirements to integration tests and maintained bidirectional traceability. The portable reference model is fully tested, and I also executed the real ATK2/Athrill backend once in Docker and preserved the unedited terminal evidence.

## 6. 면접에서 숨기면 안 되는 한계

- 현재 자동 테스트가 검증한 것은 **호스트 참조 모델**이며, 실제 TOPPERS ATK2/A-COMSTACK/Athrill 경로는 2026-09-20 Docker 실행 1회로 검증되었습니다(`evidence/atk2-athrill.log`).
- 실제 TOPPERS ATK2/A-COMSTACK/Athrill 경로는 핀 고정 커밋·재현 절차대로 Docker에서 1회 실행하고 미편집 로그를 보존했습니다. CI 회귀는 아닙니다.
- AUTOSAR 적합성, ISO 26262 인증, 양산 가능성을 주장하지 않습니다.
- 다음 증분은 Athrill 실행의 CI 회귀화 → sequence counter wrap 테스트 → AUTOSAR E2E/UDS 진단 확장 순서입니다.
